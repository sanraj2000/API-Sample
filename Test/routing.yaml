#
# Copyright Super iPaaS Integration LLC, an IBM Company 2024
#
kind: Route
apiVersion: api.webmethods.io/beta
metadata:
  name: routing
  version: alpha
  namespace: dev_all
  tags:
    - routing
    - route
    - policy
spec:
  default-endpoint:
    $ref: dev_all:endpoint:alpha